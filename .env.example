# Set the canonical public origin in production, without a trailing slash.
NEXT_PUBLIC_SITE_URL=https://your-domain.example

# Public browser key restricted to the production and preview domains in MapTiler.
NEXT_PUBLIC_MAPTILER_API_KEY=your-maptiler-key
